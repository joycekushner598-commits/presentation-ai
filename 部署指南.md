# Presentation AI 本地部署指南

## 📋 安装前的准备工作

### 1. 安装 Node.js（必需）
- 访问 [Node.js 官网](https://nodejs.org/)
- 下载并安装 Node.js 18.x 或更高版本（推荐 LTS 版本）
- 安装后重启终端验证：`node --version`

### 2. 安装 PostgreSQL 数据库（必需）
- 访问 [PostgreSQL 官网](https://www.postgresql.org/download/)
- 下载并安装 PostgreSQL
- 记住你设置的用户名和密码
- 创建一个名为 `presentation_ai` 的数据库

### 3. 获取必要的 API 密钥

#### OpenAI API Key（必需）
- 访问 [OpenAI Platform](https://platform.openai.com/api-keys)
- 注册并创建 API 密钥

#### Google OAuth 凭据（必需，用于登录）
- 访问 [Google Cloud Console](https://console.cloud.google.com/)
- 创建项目并启用 Google+ API
- 创建 OAuth 2.0 客户端 ID
- 授权重定向 URI: `http://localhost:6100/api/auth/callback/google`

#### Together AI API（可选，用于图片生成）
- 访问 [Together AI](https://www.together.ai/)

#### UploadThing Token（可选，用于文件上传）
- 访问 [UploadThing](https://uploadthing.com/)

## 🚀 部署步骤

### 步骤 1: 安装依赖
```powershell
cd "c:\python code\presentation-ai"
npm install
```

### 步骤 2: 配置环境变量
编辑 `.env` 文件，填入你的配置：
- `DATABASE_URL`: 你的 PostgreSQL 连接字符串
- `NEXTAUTH_SECRET`: 随机生成的密钥（可以运行 `openssl rand -base64 32`）
- `GOOGLE_CLIENT_ID` 和 `GOOGLE_CLIENT_SECRET`: Google OAuth 凭据
- `OPENAI_API_KEY`: OpenAI API 密钥
- 其他可选的 API 密钥

### 步骤 3: 初始化数据库
```powershell
npm run db:push
```

### 步骤 4: 启动应用（端口 6100）
```powershell
$env:PORT=6100; npm run dev
```

### 步骤 5: 访问应用
打开浏览器访问: `http://localhost:6100`

## 📝 配置说明

### 数据库 URL 格式
```
postgresql://用户名:密码@localhost:5432/数据库名
```
例如:
```
postgresql://postgres:mypassword@localhost:5432/presentation_ai
```

### 生成 NEXTAUTH_SECRET
在 PowerShell 中运行：
```powershell
[Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Maximum 256 }))
```

## 🎯 功能特性
- ✅ AI 驱动的演示文稿生成
- ✅ 9 种内置主题
- ✅ 自定义主题创建
- ✅ 实时编辑
- ✅ 演示模式
- ✅ 自动保存

## ❓ 常见问题

### 数据库连接错误
确保 PostgreSQL 服务正在运行，并且连接字符串正确。

### API 密钥错误
检查 `.env` 文件中的 API 密钥是否正确配置。

### 端口已被占用
如果端口 6100 已被占用，可以更改为其他端口：
```powershell
$env:PORT=3000; npm run dev
```

## 📞 获取帮助
- [GitHub Issues](https://github.com/joycekushner598-commits/presentation-ai/issues)
- [Discord 社区](https://discord.gg/fsMHMhAHRV)
